// Api global client
// Promises around HTTP API
// Trigger playlistUpdate actions
// Maybe call notifications on answer ?

(function(Actions, global) {

  global.API = {
    addTrack: function(attributes){

      // CB : Action.updatePlaylist
    }
  }


})(window.PD.Actions, window.PD);